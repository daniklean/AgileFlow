image: node:alpine

stages:
  - deploy

deploy_application:
  stage: deploy
  before_script:
    - npm install
    - npm run build
  script:
    - echo "Imprime el directorio actual:"
    - pwd
    - chmod +x ./deploy_script.sh
    - ./deploy_script.sh
  only:
    - main
