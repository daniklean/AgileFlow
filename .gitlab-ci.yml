image: node:alpine

stages:
  - deploy

deploy_application:
  stage: deploy
  before_script:
    - npm install
    - npm run build
  script:
    - echo "Imprime el directorio actual:"
    - pwd
    - echo "El directorio del proyecto es: $CI_PROJECT_DIR"
    - chmod +x $CI_PROJECT_DIR/ci/deploy_script.sh
    - $CI_PROJECT_DIR/ci/deploy_script.sh
  only:
    - main
    